# LLM API access (OpenAI-compatible). Copy this file to env/.env and add your key.
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# Database access — not used in this assessment, reserved for future use.
# DATABASE_URL=
