# pdfjs is used for its text layer only, never rendering, so its optional native
# canvas dependency (which pulls a vulnerable node-pre-gyp -> tar chain) is not
# needed. Skipping optional deps keeps installs clean and audit-free.
omit=optional
